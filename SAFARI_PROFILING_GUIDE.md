# 🔍 Safari DevTools: Профилирование производительности

## 📋 Подготовка

### 1. Включи меню разработчика
1. Safari → Настройки (⌘,)
2. Вкладка "Дополнения"
3. ✅ Включи "Показывать меню «Разработка» в строке меню"

### 2. Открой DevTools
- **Способ 1**: `⌘ + ⌥ + I`
- **Способ 2**: Разработка → Показать Web Inspector

---

## 🎯 Метод 1: Timeline (Основной инструмент)

### Запись профиля:
1. **Открой вкладку "Timelines"** (⌘ + ⌥ + 2)
2. **Выбери типы записи**:
   - ✅ **Rendering Frames** — FPS и время рендеринга
   - ✅ **CPU Usage** — загрузка процессора
   - ✅ **Memory** — использование памяти
   - ✅ **JavaScript & Events** — выполнение JS
3. **Нажми красную кнопку "Record"** (или `⌘ + R`)
4. **Проскролль страницу** несколько раз
5. **Останови запись** (нажми кнопку ещё раз)

### Анализ результатов:

#### 📊 **Rendering Frames** (самое важное!)
- **Зелёные полоски** — кадры <16.67ms (60 FPS)
- **Жёлтые полоски** — кадры 16.67-33ms (30-60 FPS)
- **Красные полоски** — кадры >33ms (<30 FPS)

**Что смотреть:**
- Высота полосок = время кадра
- Пробелы = пропущенные кадры
- Клик на полоску → детали кадра

#### 🔥 **CPU Usage**
Показывает, что потребляет процессор:
- **Script** (синий) — выполнение JavaScript
- **Layout** (фиолетовый) — расчёт позиций элементов
- **Painting** (зелёный) — отрисовка пикселей
- **Rendering** (оранжевый) — композиция слоёв

**Что искать:**
- Высокие пики Script → оптимизировать JS
- Высокие пики Layout → уменьшить reflow
- Высокие пики Painting → упростить визуальные эффекты

#### 💾 **Memory**
- **JavaScript Heap** — память JS объектов
- **DOM Nodes** — количество элементов на странице

**Что искать:**
- Постоянный рост памяти = утечка памяти
- Резкие скачки = создание/удаление объектов

---

## 🎯 Метод 2: JavaScript Profiler

### Запуск:
1. **Открой вкладку "Timelines"**
2. **Выбери только "JavaScript & Events"**
3. **Record → Scroll → Stop**

### Анализ:
- **Call Tree** — дерево вызовов функций
- **Self Time** — время выполнения самой функции
- **Total Time** — время с учётом вложенных вызовов

**Что искать:**
- Функции с высоким Self Time
- Частые вызовы маленьких функций
- Рекурсивные вызовы

---

## 🎯 Метод 3: Network (для изображений)

### Запуск:
1. **Открой вкладку "Network"** (⌘ + ⌥ + 3)
2. **Перезагрузи страницу** (⌘ + R)
3. **Проскролль страницу**

### Анализ:
**Фильтр "Images":**
- Размер файлов (Size)
- Время загрузки (Time)
- Порядок загрузки (Waterfall)

**Что искать:**
- Большие изображения (>500KB)
- Медленная загрузка (>1s)
- Много мелких запросов

---

## 🎯 Метод 4: Layers (для Canvas/WebGL)

### Запуск:
1. **Открой вкладку "Layers"**
2. **Проскролль страницу**
3. **Посмотри список слоёв**

### Анализ:
- **Количество слоёв** — чем меньше, тем лучше
- **Размер слоёв** — большие слои = больше памяти
- **Композиция** — частые изменения = медленный рендеринг

**Что искать:**
- Много маленьких слоёв (>50)
- Огромные слои (>2000x2000px)
- Постоянно меняющиеся слои

---

## 📊 Что записать для анализа:

### 1. **Timeline Summary** (после записи):
```
Total Time: X.XXs
Script: XX%
Layout: XX%
Painting: XX%
Rendering: XX%
```

### 2. **Rendering Frames** (кликни на красный кадр):
```
Frame Duration: XXms
FPS: XX
Layout: XXms
Paint: XXms
Composite: XXms
```

### 3. **Top Functions** (из JavaScript Profiler):
```
Function Name | Self Time | Total Time | Calls
--------------|-----------|------------|------
functionName  | XXms      | XXms       | XXX
```

### 4. **Memory Usage**:
```
JavaScript Heap: XX MB
DOM Nodes: XXXX
```

### 5. **Network (Images)**:
```
Image Name          | Size    | Time
--------------------|---------|------
image1.png          | XXX KB  | XXXms
```

---

## 🎯 Быстрый чеклист для скриншота:

1. **Timeline → Record → Scroll → Stop**
2. **Скриншот всей вкладки Timeline** (⌘ + Shift + 4)
3. **Кликни на самый высокий красный кадр**
4. **Скриншот деталей кадра**
5. **Переключись на "JavaScript & Events"**
6. **Скриншот Call Tree** (топ-10 функций)

---

## 💡 Полезные советы:

### Горячие клавиши:
- `⌘ + R` — начать/остановить запись
- `⌘ + K` — очистить консоль
- `⌘ + F` — поиск в Timeline

### Фильтры:
- Клик на легенду → скрыть/показать тип активности
- Drag по Timeline → зум на участок
- Scroll → перемещение по Timeline

### Экспорт:
- File → Export Timeline → сохранить JSON
- Можно импортировать позже для анализа

---

## 🚀 Что делать после профилирования:

### Если высокий **Script** (>50%):
- Оптимизировать JavaScript
- Кешировать результаты вычислений
- Использовать Web Workers

### Если высокий **Layout** (>30%):
- Уменьшить reflow
- Использовать `transform` вместо `top/left`
- Избегать чтения layout properties во время записи

### Если высокий **Painting** (>30%):
- Упростить тени и эффекты
- Уменьшить размер изображений
- Использовать `will-change` CSS

### Если высокий **Memory** (>500MB):
- Проверить утечки памяти
- Уменьшить количество DOM элементов
- Оптимизировать изображения

---

## 📝 Формат для отправки результатов:

```markdown
## Safari Performance Profile

### Environment:
- Safari Version: XX.X
- macOS Version: XX.X
- Screen Resolution: XXXXxXXXX

### Timeline Summary:
- Total Recording Time: X.XXs
- Script: XX%
- Layout: XX%
- Painting: XX%
- Rendering: XX%

### Rendering Frames:
- Average FPS: XX
- Worst Frame: XXms
- Total Frames: XXX
- Dropped Frames: XX

### Top 5 Functions (by Self Time):
1. functionName1 - XXms (XXX calls)
2. functionName2 - XXms (XXX calls)
3. functionName3 - XXms (XXX calls)
4. functionName4 - XXms (XXX calls)
5. functionName5 - XXms (XXX calls)

### Memory:
- JavaScript Heap: XX MB
- DOM Nodes: XXXX

### Screenshots:
[Прикрепить скриншоты Timeline и деталей кадра]
```

---

## ✅ Готово!

Теперь у тебя есть полное руководство по профилированию в Safari DevTools. Удачи! 🚀

